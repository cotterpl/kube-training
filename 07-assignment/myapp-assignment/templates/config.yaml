apiVersion: v1
kind: ConfigMap
metadata:
  name: myapp-config
data:
  redis_host: "myapp-{{.Release.Namespace}}-redis-master"
---
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: myapp-secrets
data:
  redis_password: {{ .Values.redis.auth.password | b64enc }}
